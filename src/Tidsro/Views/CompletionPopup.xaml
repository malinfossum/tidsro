<Window x:Class="Tidsro.Views.CompletionPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowActivated="False" Topmost="True" ShowInTaskbar="False"
        SizeToContent="WidthAndHeight" ResizeMode="NoResize"
        AutomationProperties.Name="{Binding AnnouncementText}"
        FontFamily="{StaticResource FontSans}">
  <!-- ElevatedBg, per spec §5: this is the only surface in the app that floats over every other
       window, so if anything is the top of the ladder it is this. Every pair it introduces is
       already measured and shipping on the next-alarm row, which is the same surface carrying the
       same QuietAction buttons - Text 12.98, TextMuted 6.36, TextFaint 4.64, BorderControl 3.27. -->
  <Border x:Name="Root" Background="{StaticResource ElevatedBg}" CornerRadius="{StaticResource RadiusMd}"
          BorderBrush="{StaticResource Border}" BorderThickness="1"
          Padding="{StaticResource CardPadding}" Width="320">
    <Border.Effect>
      <DropShadowEffect BlurRadius="30" ShadowDepth="10" Direction="270" Opacity="0.30" Color="#000000"/>
    </Border.Effect>
    <Border.RenderTransform><TranslateTransform Y="12"/></Border.RenderTransform>
    <StackPanel>
      <DockPanel>
        <TextBlock Foreground="{StaticResource TextFaint}" FontSize="{StaticResource TextXs}" VerticalAlignment="Center">
          <Run FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE73E;"/><Run Text="{Binding HeaderText, Mode=OneWay}"/>
        </TextBlock>
        <Button x:Name="DismissX" DockPanel.Dock="Right" Content="&#xE711;"
                FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                Command="{Binding DismissCommand}"
                AutomationProperties.Name="Dismiss" ToolTip="Dismiss"
                Background="Transparent" BorderThickness="0"
                Foreground="{StaticResource TextMuted}" FontSize="{StaticResource TextMd}"
                Padding="6" Cursor="Hand" HorizontalAlignment="Right"/>
      </DockPanel>
      <TextBlock Text="{Binding Title}" Foreground="{StaticResource Text}"
                 FontSize="{StaticResource TextLg}" FontWeight="SemiBold"
                 TextWrapping="Wrap" Margin="0,6,0,16"/>
      <!-- Actions: hidden for mouse-at-rest, shown on hover OR when the card has keyboard focus -->
      <StackPanel x:Name="Actions" Orientation="Horizontal" Opacity="0">
        <Button Content="+5 min" Command="{Binding Plus5Command}"
                AutomationProperties.Name="Add 5 minutes" Style="{StaticResource QuietAction}"
                Visibility="{Binding ShowSnooze, Converter={StaticResource BoolToVisible}}"/>
        <Button Content="Restart" Command="{Binding RestartCommand}"
                AutomationProperties.Name="Restart timer" Style="{StaticResource QuietAction}" Margin="8,0,0,0"
                Visibility="{Binding ShowRestart, Converter={StaticResource BoolToVisible}}"/>
        <Button Content="Dismiss" Command="{Binding DismissCommand}"
                AutomationProperties.Name="Dismiss" Style="{StaticResource QuietAction}" Margin="8,0,0,0"/>
      </StackPanel>
    </StackPanel>
  </Border>
</Window>
